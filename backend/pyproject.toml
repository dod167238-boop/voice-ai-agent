[project]
name = "voice-ai-agent"
version = "1.0.0"
description = "Voice AI Agent"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]",
    "uvicorn",
    "requests",
    "python-multipart"
]

[tool.fastapi]
entrypoint = "main:app"

[tool.setuptools]
py-modules = []